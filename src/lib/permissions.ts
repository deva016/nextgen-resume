// All features are now available to all users without subscription restrictions

export function canCreateResume() {
  // Allow unlimited resumes for all users
  return true;
}

export function canUseAITools() {
  // AI tools available to all users
  return true;
}

export function canUseCustomizations() {
  // Customizations available to all users
  return true;
}
